// 1)  Use the var keyword to create a variable called myName.

// Example
var ourName;

// Answer
var myName;

// ####################

// 2 ) Assign the value 7 to variable a. Assign the contents of a to variable b.

// Setup
var a;
var b = 2;

// Answer
var a = 7;
var b = a; 

// ######################

// 3) Define a variable a with var and initialize it to a value of 9.

// Example
var ourVar = 19;

// Answer
var a = 9;
console.log(a);


// ##########################

// 4) Initialize the three variables a, b, and c with 5, 10, and "I am a" respectively so that they will not be undefined.

// Answer
var a = 5;
var b = 10;
var c = 'I am a';

// Do not change code below this line

a = a + 1;
b = b + 5;
c = c + " String!";

// ###############################

// 5) Modify the existing declarations and assignments so their names use camelCase.
// Do not create any new variables.

// Declarations
var studlyCapVar;
var properCamelCase;
var titleCaseOver;

// Assignments
studlyCapVar = 10;
properCamelCase = "A String";
titleCaseOver = 9000;

// ###########################

// 6) Change the 0 so that sum will equal 20.

// Answer
var sum = 10 + 10;
console.log(sum);


// ################################

// 7) Change the 0 so the difference is 12.

// Answer
var difference = 45 - 33;
console.log(difference);


// #################################

// 8) Change the 0 so that product will equal 80.

// Answer
var product = 8 * 10;
console.log(product);


// #############################

// 9) Change the 0 so that the quotient is equal to 2.

// Answer 
var quotient = 66 / 33;
console.log(quotient);


// #############################

// 10) Change the code to use the ++ operator on myVar.

var myVar = 87;

// Answer
 myVar ++;
 console.log(myVar);
 

// ###############################

// 11) Change the code to use the -- operator on myVar.

var myVar1 = 11;

// Answer
myVar1--;
console.log(myVar1);


// ###############################

// 12) Create a variable myDecimal and give it a decimal value with a fractional part (e.g. 5.7).

var ourDecimal = 5.7;

// Answer
var myDecimal = 4.1;
console.log(myDecimal);

// #################################

// 13) Change the 0.0 so that product will equal 5.0.

// Answer
var product = 2.0 * 2.5;
console.log(product);

// #################################

// 14) Change the 0.0 so that quotient will equal to 2.2.

// Answer
var quotient = 4.4 / 2.0; 
console.log(quotient);

// #################################

// 15) Set remainder equal to the remainder of 11 divided by 3 using the remainder (%) operator.

// Answer
var remainder = 11 % 3;
console.log(remainder);

// #################################
// 16) Convert the assignments for a, b, and c to use the += operator.

var a = 3;
var b = 17;
var c = 12;

// Answer

a+= 12;
b+= 9;
c+= 7;
// #################################
// #################################
// #################################

